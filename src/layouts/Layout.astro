---
import { useTranslations } from "../i18n/utils";

interface Props {
  title?: string;
  lang: string;
}

const { title = "My Website", lang } = Astro.props;
const t = useTranslations(lang as "en" | "es");
---

<html lang={lang}>
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="description" content="My multilingual website" />
    <title>{title}</title>
  </head>
  <body>
    <header>
      <nav>
        <a href="/en/">English</a>
        <a href="/es/">Español</a>
      </nav>
    </header>
    <main>
      <slot />
    </main>
    <footer>
      <p>&copy; {new Date().getFullYear()}</p>
    </footer>
  </body>
</html>
