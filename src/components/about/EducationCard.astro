---
interface Props {
  title: string;
  degree: string;
  period: string;
  description: string;
}

const { title, degree, period, description } = Astro.props;
---

<div class="space-y-6 md:space-y-8">
  <h2 class="text-xl md:text-2xl lg:text-3xl font-playfair font-bold tracking-tight">{title}</h2>
  
  <div class="space-y-8 md:space-y-10">
    <div class="group relative grid pb-1 transition-all sm:grid-cols-8 sm:gap-6 md:gap-8">
      <div class="absolute -inset-x-4 -inset-y-4 z-0 hidden rounded-md transition motion-reduce:transition-none lg:-inset-x-6 lg:block"></div>
      
      <header class="z-10 mb-2 mt-1 text-xs md:text-sm font-semibold uppercase tracking-wide text-base-content/50 sm:col-span-2">
        {period}
      </header>
      
      <div class="z-10 sm:col-span-6">
        <h3 class="font-medium leading-snug text-base-content">
          <div>
            <span class="text-base md:text-lg lg:text-xl font-semibold">{degree}</span>
          </div>
        </h3>
        
        <p class="mt-2 text-xs md:text-sm lg:text-base leading-relaxed text-base-content/80">
          {description}
        </p>
      </div>
    </div>
  </div>
</div>